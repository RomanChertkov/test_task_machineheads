import { FC } from 'react'
import { Typography } from 'antd'

const { Title } = Typography
interface TagsPageProps {}

const TagsPage: FC<TagsPageProps> = ({}) => {
  return <Title>Теги</Title>
}
export default TagsPage
